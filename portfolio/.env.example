# Base URL of the FastAPI backend (the one that serves /nlp/ask).
# Local development: run the backend with
#   cd ../backend && uvicorn main:app --port 8000
# and set this to http://localhost:8000
NEXT_PUBLIC_API_URL=https://ai-interactive-portfolio-back-end.vercel.app
